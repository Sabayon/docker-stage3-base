machine:
  services:
    - docker
dependencies:
  override:
    - docker info
test:
  override:
    - ./update.sh
deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push sabayon/gentoo-stage3-base-amd64
